# Label Flow - 标注任务平台

## 需求
实现一个标注任务平台
背景: 我们提供一个bucket, 里面有一些视频和图片, 基于这些数据搭建支持「权限管理+标注+质检」的web平台
核心需求:
1. 角色权限：
  - 管理员（用户管理、任务包创建/分包）
  - 标注员（领取未标注任务、标注）
  - 质检员（领取已标注任务、质检）
2. 任务管理：媒体资源（图片/视频）自动编号、批量分包，任务状态流转（未标注→已标注→质检中→质检通过/驳回）
3. 标注功能：
   - 媒体展示：图片/视频播放、缩放（鼠标滚轮）
   - 标注工具：矩形框选、椭圆框选（支持拖拽调整大小）
   - 附加信息：备注问题、1-5分质量打分 （质检员）
   - 视频处理：截取任意时间段帧、画面裁切
   - 任务追溯: 保存标注过后的任务和meta信息, 同时记得绑定标注人员和质检人员
如能提供设计架构图为加分项

## 📋 项目概述

Label Flow 是一个支持「权限管理 + 标注 + 质检」的 Web 标注任务平台，基于 AWS S3 存储的媒体资源（图片/视频），提供完整的标注工作流管理

---

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   前端层 (Frontend)                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                         Next.js 16 (App Router)                         │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │    │
│  │  │   登录模块    │   │   管理员     │  │   标注员      │  │   质检员     │  │    │
│  │  │  Auth Pages  │  │  Dashboard   │  │  Workspace   │  │  Workspace  │  │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │    │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │    │
│  │  │                      Canvas 标注引擎 (Fabric.js         )           │  │    │
│  │  │   • 矩形框选  • 椭圆框选  • 拖拽调整  • 缩放平移  • 视频帧截取            │ │    │
│  │  └────────────────────────────────────────────────────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │ HTTPS / WebSocket
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   API 网关层                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                    Next.js API Routes / Route Handlers                  │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │    │
│  │  │  Auth API    │  │  Task API    │  │ Annotation   │  │             │  │    │
│  │  │  /api/auth   │  │  /api/tasks  │  │    API       │  │              │  │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                        │                                        │
│  ┌─────────────────────────────────────┴────────────────────────────────┐       │
│  │                          中间件层 (Middleware)                         │       │
│  │      • JWT 验证  • 角色权限校验 (RBAC)  • 请求日志  • 限流                │       │
│  └──────────────────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    ▼                   ▼                   ▼
┌───────────────────────┐  ┌───────────────────┐  ┌───────────────────────────┐
│     数据存储层         │  │    文件存储层      │  │      缓存层                 │
│  ┌─────────────────┐  │  │  ┌─────────────┐  │  │  ┌─────────────────────┐  │
│  │   PostgreSQL    │  │  │  │   AWS S3    │  │  │  │   Redis (可选)       │  │
│  │  ─────────────  │  │  │  │  ─────────  │  │  │  │  ─────────────────  │  │
│  │  • 用户表       │  │  │  │  • 图片     │  │  │   │  • 会话管理            │  │
│  │  • 任务包表     │  │  │  │  • 视频     │  │  │  │  • 任务状态缓存         │  │
│  │  • 任务表       │  │  │  │  • 截图     │  │  │  │  • 热点数据缓存         │  │
│  │  • 标注数据表   │  │  │  └─────────────┘  │  │  └─────────────────────┘  │
│  │  • 操作日志表   │  │  │  Bucket:          │  │                           │
│  └─────────────────┘  │  │  interviewonly    │  │                           │
│       (Prisma ORM)    │  │  ap-southeast-2   │  │                           │
└───────────────────────┘  └───────────────────┘  └───────────────────────────┘
```

---

## 🔄 业务流程图

### 核心工作流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           任务状态流转（优化版）                              │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │  S3 媒体库   │  管理员同步
    │ (图片/视频)  │  ────────────┐
    └──────────────┘              │
                                  ▼
                           ┌──────────────┐
                           │  自动编号     │
                           │  media_001~N │
                           └──────┬───────┘
                                  │ 管理员创建任务包
                                  ▼
    ╔══════════════════════════════════════════════════════════════════════╗
    ║                         任务包 (Package)                              ║
    ║  包含多个任务，支持批量操作，状态统计（总数/进行中/已完成等）          ║
    ╚══════════════════════════════════════════════════════════════════════╝
                                  │
         ┌────────────────────────┼────────────────────────┐
         ▼                        ▼                        ▼
    ┌─────────┐              ┌─────────┐              ┌─────────┐
    │ Task 1  │              │ Task 2  │      ...     │ Task N  │
    │ PENDING │              │ PENDING │              │ PENDING │
    └─────────┘              └─────────┘              └─────────┘
         │                        │                        │
         └────────────────────────┴────────────────────────┘
                                  │
                                  │ 标注员领取任务包
                                  │ (批量领取所有PENDING/REJECTED)
                                  ▼
                           ┌──────────────┐
                           │   LABELING   │ ◀─┐ 自动跳转下一任务
                           │   (标注中)    │   │ (包内连续标注)
                           └──────┬───────┘   │
                                  │           │
                                  │ 提交标注   │
                                  ▼           │
                           ┌─────────────────┴─────────┐
                           │   智能分配逻辑              │
                           │ • 若该包已有质检员 → CHECKING │
                           │ • 若无质检员      → LABELED  │
                           └──────┬────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    ▼                           ▼
            ┌──────────────┐            ┌──────────────┐
            │   LABELED    │            │   CHECKING   │
            │  (已标注)     │            │  (质检中)     │
            └──────┬───────┘            └──────┬───────┘
                   │                           │
                   │ 质检员领取任务包            │ 质检操作
                   │ (批量领取LABELED)          │
                   ▼                           ▼
            ┌──────────────┐         ┌────────┴────────┐
            │   CHECKING   │         │   质检评分      │
            │  (质检中)     │         │   (1-5分)      │
            └──────┬───────┘         └────────┬────────┘
                   │                          │
                   └──────────────┬───────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    ▼ 通过                      ▼ 驳回
            ┌──────────────┐            ┌──────────────┐
            │   APPROVED   │            │   REJECTED   │
            │  (已通过)     │            │  (已驳回)     │
            └──────────────┘            └──────┬───────┘
                                               │
                                               │ 重新标注
                                               │ (回到标注员队列)
                                               └──────────────┐
                                                             ▼
                                                      ┌──────────────┐
                                                      │  (待重新标注)  │
                                                      └──────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                              关键流程说明                                     ║
╠══════════════════════════════════════════════════════════════════════════════╣
║ 1. 任务包级别操作                                                             ║
║    • 标注员/质检员按包领取，一次领取包内所有可操作任务                         ║
║    • 领取后自动跳转到第一个任务，完成后继续包内其他任务                        ║
║    • 支持按状态筛选：全部/标注中/已标注/已驳回/已通过                          ║
║                                                                              ║
║ 2. 智能任务分配                                                               ║
║    • 标注员提交后，若该包已有质检员领取 → 直接进入CHECKING                     ║
║    • 否则进入LABELED状态，等待质检员领取                                       ║
║    • 提交成功后自动跳转到下一个PENDING任务（无则返回列表）                     ║
║                                                                              ║
║ 3. 质检流程                                                                   ║
║    • 质检员可查看CHECKING状态任务，继续未完成的质检                            ║
║    • 评分1-5分，可添加备注反馈                                                ║
║    • 驳回任务回到PENDING状态，分配给原标注员重新处理                           ║
║                                                                              ║
║ 4. 状态追溯                                                                   ║
║    • 所有状态变更记录在operation_logs表                                       ║
║    • 保存标注员、质检员信息，支持审计追踪                                      ║
║    • 质检评分保存在quality_scores表，可查看历史评分                           ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

### 用户角色权限矩阵

```
┌────────────────────┬────────────┬────────────┬────────────┐
│       功能         │   管理员   │   标注员   │   质检员   │
├────────────────────┼────────────┼────────────┼────────────┤
│ 用户管理           │     ✓      │     ✗      │     ✗      │
│ 创建任务包         │     ✓      │     ✗      │     ✗      │
│ 批量分包           │     ✓      │     ✗      │     ✗      │
│ 查看所有任务       │     ✓      │     ✗      │     ✗      │
│ 领取未标注任务     │     ✗      │     ✓      │     ✗      │
│ 执行标注操作       │     ✗      │     ✓      │     ✗      │
│ 领取已标注任务     │     ✗      │     ✗      │     ✓      │
│ 执行质检操作       │     ✗      │     ✗      │     ✓      │
│ 驳回/通过任务      │     ✗      │     ✗      │     ✓      │
│ 查看统计报表       │     ✓      │     ✓      │     ✓      │
└────────────────────┴────────────┴────────────┴────────────┘
```

---

## 📊 数据库设计 (ER 图)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              数据库表结构设计                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐
│        users         │         │        roles         │
├──────────────────────┤         ├──────────────────────┤
│ id (PK)              │    ┌───▶│ id (PK)              │
│ username             │    │    │ name                 │
│ email                │    │    │ permissions (JSON)   │
│ password_hash        │    │    │ created_at           │
│ role_id (FK) ────────┼────┘    └──────────────────────┘
│ status               │
│ created_at           │
│ updated_at           │
└──────────────────────┘
         │
         │ 1:N
         ▼
┌──────────────────────┐         ┌──────────────────────┐
│    task_packages     │         │    media_resources   │
├──────────────────────┤         ├──────────────────────┤
│ id (PK)              │    ┌───▶│ id (PK)              │
│ name                 │    │    │ s3_key               │
│ description          │    │    │ s3_url               │
│ created_by (FK)──────┼────┘    │ type (image/video)   │
│ status               │         │ file_name            │
│ total_count          │         │ file_size            │
│ created_at           │         │ duration (视频时长)   │
│ updated_at           │         │ width                │
└──────────────────────┘         │ height               │
         │                       │ auto_number          │
         │ 1:N                   │ created_at           │
         ▼                       └──────────────────────┘
┌──────────────────────┐                  │
│        tasks         │                  │
├──────────────────────┤                  │
│ id (PK)              │◀─────────────────┘
│ package_id (FK)      │         1:1
│ media_id (FK)        │
│ status               │──────────────────────────────────┐
│ labeler_id (FK)      │  (PENDING/LABELING/LABELED/      │
│ checker_id (FK)      │   CHECKING/APPROVED/REJECTED)    │
│ assigned_at          │                                  │
│ labeled_at           │                                  │
│ checked_at           │                                  │
│ created_at           │                                  │
│ updated_at           │                                  │
└──────────────────────┘                                  │
         │                                                │
         │ 1:N                                            │
         ▼                                                │
┌──────────────────────┐                                  │
│     annotations      │                                  │
├──────────────────────┤                                  │
│ id (PK)              │                                  │
│ task_id (FK)         │                                  │
│ type (rect/ellipse)  │                                  │
│ coordinates (JSON)   │◀─ { x, y, width, height, rotation }
│ label                │                                  │
│ frame_time (视频帧)  │                                  │
│ created_by (FK)      │                                  │
│ created_at           │                                  │
│ updated_at           │                                  │
└──────────────────────┘                                  │
                                                          │
┌──────────────────────┐         ┌──────────────────────┐ │
│   task_metadata      │         │    quality_scores    │ │
├──────────────────────┤         ├──────────────────────┤ │
│ id (PK)              │         │ id (PK)              │ │
│ task_id (FK)         │         │ task_id (FK)         │ │
│ remarks              │         │ score (1-5)          │ │
│ video_clips (JSON)   │◀─ 视频截取片段信息              │ │
│ cropped_areas (JSON) │◀─ 画面裁切区域                  │ │
│ created_at           │         │ created_by (FK)      │ │
│ updated_at           │         │ created_at           │ │
└──────────────────────┘         └──────────────────────┘ │
                                                          │
┌──────────────────────┐                                  │
│    operation_logs    │◀─────────────────────────────────┘
├──────────────────────┤   记录所有状态变更
│ id (PK)              │
│ task_id (FK)         │
│ user_id (FK)         │
│ action               │
│ old_status           │
│ new_status           │
│ details (JSON)       │
│ created_at           │
└──────────────────────┘
```

### 数据库索引设计

```sql
-- 关键索引
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_labeler ON tasks(labeler_id);
CREATE INDEX idx_tasks_checker ON tasks(checker_id);
CREATE INDEX idx_tasks_package ON tasks(package_id);
CREATE INDEX idx_annotations_task ON annotations(task_id);
CREATE INDEX idx_media_auto_number ON media_resources(auto_number);
CREATE INDEX idx_logs_task ON operation_logs(task_id);
CREATE INDEX idx_logs_user ON operation_logs(user_id);
```

---

## 🖥️ 前端架构设计

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              前端目录结构                                        │
└─────────────────────────────────────────────────────────────────────────────────┘

src/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 认证相关页面组
│   │   ├── login/
│   │   └── register/
│   ├── (dashboard)/              # 主应用页面组
│   │   ├── admin/                # 管理员模块
│   │   │   ├── users/            # 用户管理
│   │   │   ├── packages/         # 任务包管理
│   │   │   └── statistics/       # 统计报表
│   │   ├── labeler/              # 标注员模块
│   │   │   └── workspace/        # 标注工作台
│   │   │       ├── page.tsx      # 任务包列表 (可领取/我的)
│   │   │       └── [id]/         # 具体标注页面
│   │   └── checker/              # 质检员模块
│   │       └── workspace/        # 质检工作台
│   │           ├── page.tsx      # 任务包列表 (待质检/我的)
│   │           └── [id]/         # 具体质检页面
│   ├── api/                      # API Routes
│   │   ├── auth/
│   │   ├── tasks/
│   │   ├── annotations/
│   │   ├── packages/
│   │   └── s3/
│   └── layout.tsx
│
├── components/
│   ├── annotation/               # 标注相关组件
│   │   ├── Canvas.tsx            # 画布主组件
│   │   ├── RectTool.tsx          # 矩形工具
│   │   ├── EllipseTool.tsx       # 椭圆工具
│   │   ├── Toolbar.tsx           # 工具栏
│   │   └── ShapeEditor.tsx       # 形状编辑器
│   ├── media/                    # 媒体相关组件
│   │   ├── ImageViewer.tsx       # 图片查看器
│   │   ├── VideoPlayer.tsx       # 视频播放器
│   │   ├── FrameExtractor.tsx    # 帧提取器
│   │   └── ZoomControls.tsx      # 缩放控制
│   ├── task/                     # 任务相关组件
│   │   ├── TaskCard.tsx
│   │   ├── TaskList.tsx
│   │   └── TaskDetail.tsx
│   └── ui/                       # 通用UI组件
│
├── hooks/                        # 自定义 Hooks
│   ├── useCanvas.ts              # 画布操作
│   ├── useAnnotation.ts          # 标注逻辑
│   ├── useVideoFrame.ts          # 视频帧处理
│   └── useZoom.ts                # 缩放逻辑
│
├── lib/
│   ├── auth/                     # 认证相关
│   ├── s3/                       # S3 操作
│   └── prisma/                   # 数据库客户端
│
└── types/                        # TypeScript 类型定义
    ├── user.ts
    ├── task.ts
    └── annotation.ts
```

### 标注引擎架构

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           Canvas 标注引擎架构                                    │
└─────────────────────────────────────────────────────────────────────────────────┘

                        ┌─────────────────────────┐
                        │     AnnotationCanvas    │
                        │     (主容器组件)         │
                        └────────────┬────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         ▼                           ▼                           ▼
┌─────────────────┐        ┌─────────────────┐        ┌─────────────────┐
│   MediaLayer    │        │  AnnotationLayer │        │   ControlLayer  │
│   (媒体底层)    │        │   (标注层)       │        │   (控制层)      │
├─────────────────┤        ├─────────────────┤        ├─────────────────┤
│ • 图片渲染      │        │ • 矩形绘制       │        │ • 工具栏        │
│ • 视频播放      │        │ • 椭圆绘制       │        │ • 属性面板      │
│ • 缩放处理      │        │ • 形状选择       │        │ • 快捷键        │
│ • 平移处理      │        │ • 拖拽调整       │        │ • 撤销/重做     │
└─────────────────┘        └─────────────────┘        └─────────────────┘
         │                           │                           │
         └───────────────────────────┼───────────────────────────┘
                                     ▼
                        ┌─────────────────────────┐
                        │     CanvasEngine        │
                        │   (Fabric.js/Konva.js)  │
                        └─────────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                ▼                ▼
            ┌───────────┐    ┌───────────┐    ┌───────────┐
            │  事件系统  │    │  渲染系统  │    │  序列化   │
            │ • 鼠标     │    │ • WebGL   │    │ • JSON    │
            │ • 触摸     │    │ • Canvas  │    │ • 导出    │
            │ • 键盘     │    │ • SVG     │    │ • 恢复    │
            └───────────┘    └───────────┘    └───────────┘
```

---

## 🔌 API 接口设计

### RESTful API 规范

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              API 接口列表                                        │
└─────────────────────────────────────────────────────────────────────────────────┘

认证模块 (/api/auth)
├── POST   /api/auth/login              # 用户登录
├── POST   /api/auth/logout             # 用户登出
├── GET    /api/auth/me                 # 获取当前用户信息
└── POST   /api/auth/refresh            # 刷新Token

用户管理 (/api/users) - 仅管理员
├── GET    /api/users                   # 获取用户列表
├── POST   /api/users                   # 创建用户
├── GET    /api/users/:id               # 获取用户详情
├── PATCH  /api/users/:id               # 更新用户
└── DELETE /api/users/:id               # 删除用户

任务包管理 (/api/packages)
├── GET    /api/packages                # 获取任务包列表 (支持角色过滤)
│                                       # Query: filter=available|my (标注员/质检员)
├── POST   /api/packages                # 创建任务包 (仅管理员)
├── GET    /api/packages/:id            # 获取任务包详情
├── PATCH  /api/packages/:id            # 更新任务包 (仅管理员)
├── DELETE /api/packages/:id            # 删除任务包 (仅管理员)
├── POST   /api/packages/:id/distribute # 批量分配任务 (仅管理员)
│                                       # Body: { limit: number } (max 1000)
└── POST   /api/packages/:id/claim      # 领取任务包 (标注员/质检员)
                                        # 批量领取包内所有可操作任务

媒体资源 (/api/media)
├── GET    /api/media                   # 获取媒体列表
├── GET    /api/media/:id               # 获取媒体详情
├── GET    /api/media/sync              # 同步S3资源
└── GET    /api/media/:id/signed-url    # 获取签名URL

任务管理 (/api/tasks)
├── GET    /api/tasks                   # 获取任务列表 (按角色过滤)
│                                       # Query: myTasks=true (仅我的任务)
│                                       # Query: packageId=xxx (指定包)
│                                       # Query: status=xxx (状态过滤)
├── GET    /api/tasks/:id               # 获取任务详情
├── POST   /api/tasks/:id/claim         # 领取单个任务 (已弃用，使用包级领取)
├── POST   /api/tasks/:id/submit        # 提交标注
├── POST   /api/tasks/:id/review        # 质检操作 (approve/reject)
└── GET    /api/tasks/:id/history       # 获取任务历史

标注管理 (/api/annotations)
├── GET    /api/annotations/:taskId     # 获取任务标注
├── POST   /api/annotations             # 创建标注
├── PATCH  /api/annotations/:id         # 更新标注
├── DELETE /api/annotations/:id         # 删除标注
└── POST   /api/annotations/batch       # 批量保存标注


统计报表 (/api/statistics)
├── GET    /api/statistics/overview     # 总览数据
├── GET    /api/statistics/tasks        # 任务统计
└── GET    /api/statistics/users        # 用户工作量统计
```

### 核心接口数据结构

```typescript
// 任务状态枚举
enum TaskStatus {
  PENDING = 'PENDING',       // 未分配
  LABELING = 'LABELING',     // 标注中
  LABELED = 'LABELED',       // 已标注
  CHECKING = 'CHECKING',     // 质检中
  APPROVED = 'APPROVED',     // 质检通过
  REJECTED = 'REJECTED'      // 质检驳回
}

// 标注数据结构
interface Annotation {
  id: string;
  taskId: string;
  type: 'rect' | 'ellipse';
  coordinates: {
    x: number;
    y: number;
    width: number;
    height: number;
    rotation?: number;
  };
  label?: string;
  frameTime?: number;  // 视频帧时间点
  createdBy: string;
  createdAt: Date;
}

// 任务提交请求
interface TaskSubmitRequest {
  annotations: Annotation[];
  metadata: {
    remarks?: string;
    videoClips?: VideoClip[];
    croppedAreas?: CropArea[];
  };
  qualityScore?: number;  // 质检时使用
}
```

---

## 🔐 安全设计

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              安全架构设计                                        │
└─────────────────────────────────────────────────────────────────────────────────┘

                           ┌─────────────────────┐
                           │      客户端         │
                           └──────────┬──────────┘
                                      │
                                      │ HTTPS Only
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              认证与授权流程                                      │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                           JWT 认证流程                                   │    │
│  │                                                                          │    │
│  │   Login ──▶ 验证凭据 ──▶ 生成JWT ──▶ 返回Token ──▶ 存储到HttpOnly Cookie │    │
│  │                                                                          │    │
│  │   请求 ──▶ 验证JWT ──▶ 解析用户信息 ──▶ RBAC权限检查 ──▶ 业务处理        │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                           RBAC 权限模型                                  │    │
│  │                                                                          │    │
│  │   用户(User) ───1:1───▶ 角色(Role) ───1:N───▶ 权限(Permission)          │    │
│  │                                                                          │    │
│  │   Admin:  [user:*, package:*, task:read, statistics:*]                  │    │
│  │   Labeler: [task:claim, task:label, annotation:*, statistics:self]      │    │
│  │   Checker: [task:check, task:approve, task:reject, statistics:self]     │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────────┘

安全措施清单:
┌────────────────────────────────────────────────────────────────────────┐
│ ✓ JWT Token 存储在 HttpOnly Cookie，防止 XSS 攻击                      │
│ ✓ CSRF Token 防护跨站请求伪造                                          │
│ ✓ 密码使用 bcrypt 加盐哈希存储                                         │
│ ✓ S3 资源通过后端代理访问，不暴露 AWS 凭据                             │
│ ✓ API 请求限流防止 DDoS                                                │
│ ✓ 输入验证使用 Zod Schema                                              │
│ ✓ SQL 注入防护 (Prisma ORM 参数化查询)                                 │
│ ✓ 敏感操作记录审计日志                                                  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 技术栈选型

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              技术栈选型                                          │
└─────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┬──────────────────────────────────────────────────────────────┐
│      层级        │                         技术选型                             │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • Next.js 16 (App Router) - React 全栈框架                  │
│     前端框架     │  • React 19 - UI 库                                          │
│                  │  • TypeScript - 类型安全                                     │
│                  │  • Tailwind CSS 4 - 原子化 CSS                               │
│                  │  • DaisyUI - UI 组件库                                       │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • Fabric.js 7 - Canvas 标注引擎                             │
│     标注引擎     │  • 支持图形绘制、变换、事件处理                              │
│                  │  • 自定义矩形/椭圆工具组件                                   │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • React Player - 视频播放                                   │
│     媒体处理     │  • HTML5 Canvas API - 视频帧提取                             │
│                  │  • @use-gesture/react - 手势交互                             │
│                  │  • 自定义媒体预览组件 (MediaPreview)                         │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • React Context - 全局状态 (MediaPreview, Toast)           │
│     状态管理     │  • React Hooks - 组件状态管理                                │
│                  │  • 本地状态优先，避免过度抽象                                │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • Next.js API Routes - 后端 API                             │
│     后端服务     │  • Prisma ORM 7 - 数据库操作                                 │
│                  │  • AWS SDK v3 - S3 操作                                      │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • PostgreSQL - 主数据库                                     │
│     数据存储     │  • AWS S3 - 媒体文件存储                                     │
│                  │  • Prisma Adapter (pg) - 数据库连接池                        │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • jose - JWT 处理                                           │
│     认证授权     │  • bcryptjs - 密码哈希                                       │
│                  │  • Next.js Middleware - 路由守卫                             │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • uuid v13 - ID 生成                                        │
│     工具库       │  • dayjs - 日期处理                                          │
│                  │  • lucide-react - 图标库                                     │
│                  │  • tailwind-merge - 样式合并                                 │
├──────────────────┼──────────────────────────────────────────────────────────────┤
│                  │  • Vercel - 推荐部署平台                                     │
│     部署方案     │  • Docker - 容器化部署                                       │
│                  │  • Supabase / Neon - PostgreSQL 托管                         │
└──────────────────┴──────────────────────────────────────────────────────────────┘
```

---

## 📁 视频处理方案

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           视频处理架构                                           │
└─────────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                              视频帧提取流程                                    │
│                                                                               │
│   ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐               │
│   │  Video  │────▶│ Seek to │────▶│ Draw to │────▶│ Export  │               │
│   │ Element │     │  Time   │     │ Canvas  │     │  Image  │               │
│   └─────────┘     └─────────┘     └─────────┘     └─────────┘               │
│                                                                               │
│   // 帧提取代码示例                                                           │
│   const extractFrame = (video: HTMLVideoElement, time: number) => {          │
│     video.currentTime = time;                                                │
│     const canvas = document.createElement('canvas');                         │
│     canvas.width = video.videoWidth;                                         │
│     canvas.height = video.videoHeight;                                       │
│     canvas.getContext('2d').drawImage(video, 0, 0);                         │
│     return canvas.toDataURL('image/png');                                    │
│   };                                                                          │
└───────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                              画面裁切功能                                      │
│                                                                               │
│   ┌─────────────────────────────────────────────────────────┐                │
│   │                    Original Frame                        │                │
│   │  ┌───────────────────────────────────┐                  │                │
│   │  │         Crop Selection Box        │ ◀── 用户选择区域 │                │
│   │  │                                   │                  │                │
│   │  └───────────────────────────────────┘                  │                │
│   └─────────────────────────────────────────────────────────┘                │
│                           │                                                   │
│                           ▼                                                   │
│   ┌───────────────────────────────────┐                                      │
│   │         Cropped Result            │ ◀── 裁切后保存                       │
│   └───────────────────────────────────┘                                      │
└───────────────────────────────────────────────────────────────────────────────┘

视频时间段截取数据结构:
{
  "clips": [
    {
      "id": "clip-1",
      "startTime": 10.5,    // 开始时间 (秒)
      "endTime": 25.3,      // 结束时间 (秒)
      "frames": [           // 关键帧
        { "time": 10.5, "imageUrl": "...", "annotations": [...] },
        { "time": 15.0, "imageUrl": "...", "annotations": [...] }
      ]
    }
  ]
}
```

---

## 📈 扩展性设计

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              未来扩展考虑                                        │
└─────────────────────────────────────────────────────────────────────────────────┘

1. 标注工具扩展
   ├── 多边形标注
   ├── 点标注
   ├── 线段标注
   ├── 语义分割
   └── OCR 文字识别标注

2. 协作功能
   ├── 实时协作标注 (WebSocket)
   ├── 评论系统
   └── @提及通知

3. AI 辅助
   ├── 自动标注建议
   ├── 标注质量检测
   └── 异常检测预警

4. 数据导出
   ├── COCO 格式
   ├── Pascal VOC 格式
   ├── YOLO 格式
   └── 自定义格式

5. 性能优化
   ├── 图片懒加载 & 预加载
   ├── 虚拟滚动列表
   ├── WebWorker 处理大任务
   └── CDN 加速媒体访问
```

---

## 🚀 开发计划

```
Phase 1: 基础架构 (Week 1)
├── 项目初始化 & 环境配置
├── 数据库设计 & Prisma Schema
├── 认证系统 (登录/权限)
└── S3 集成 & 媒体同步

Phase 2: 核心功能 (Week 2-3)
├── 管理员：用户管理、任务包管理
├── 任务分配 & 状态流转
├── 标注工作台基础功能
└── 质检工作台基础功能

Phase 3: 标注引擎 (Week 3-4)
├── Canvas 画布实现
├── 矩形/椭圆工具
├── 缩放/平移交互
├── 视频播放 & 帧提取
└── 标注数据保存

Phase 4: 完善 & 优化 (Week 5)
├── 质量评分 & 备注功能
├── 操作日志 & 追溯
├── 统计报表
└── 性能优化 & Bug 修复
```

---

## 📝 环境配置

```bash
# .env.local 示例
DATABASE_URL="postgresql://user:password@localhost:5432/label_flow"
JWT_SECRET="your-jwt-secret-key"

# AWS S3 配置
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="ap-southeast-2"
S3_BUCKET_NAME="interviewonly"
```

---

## 📌 总结

本设计方案基于 Next.js 14 全栈框架，采用 TypeScript 保证类型安全，使用 Prisma ORM 操作 PostgreSQL 数据库，通过 AWS SDK 连接 S3 存储。

**核心设计亮点：**

1. **清晰的角色权限模型** - 基于 RBAC 的三角色系统，权限粒度可控
2. **完整的任务状态机** - 覆盖从创建到完成的全生命周期
3. **灵活的标注引擎** - 基于 Canvas 的可扩展标注工具架构
4. **完善的追溯机制** - 操作日志记录所有变更，支持审计追踪
5. **安全优先设计** - JWT + RBAC + S3 代理，多层安全防护

此架构具备良好的可扩展性，可根据业务需求持续迭代优化。

---

## 🎯 实现特点

### 1. 任务包级别操作
- **批量领取**：标注员/质检员按任务包（Package）维度领取任务，一次性领取包内所有可操作的任务
- **状态统计**：任务包卡片展示实时统计（总数、待领取、进行中、已完成）
- **工作连续性**：领取后自动跳转到第一个待处理任务，完成后可在包内继续
- **权限隔离**：
  - 标注员看到：包含 PENDING/REJECTED 任务的包
  - 质检员看到：包含 LABELED 任务的包

### 2. 用户体验优化
- **Toast 通知系统**：全局 toast 提示，支持 success/error/info/warn 类型
- **媒体预览组件**：全局 MediaPreview 组件，支持图片/视频预览、上下张切换
- **分页加载**：
  - 管理员任务包列表：9个/页
  - 标注员/质检员工作台：12个/页
  - 媒体资源列表：20个/页
- **任务包分配限制**：单次最多分配 1000 个任务，防止数据库压力
- **实时状态更新**：领取任务包后自动刷新列表，展示最新状态
- **Loading 状态**：所有异步操作都有 loading 状态提示

### 3. 数据一致性保证
- **事务处理**：任务包领取、标注提交使用 Prisma 事务，确保批量更新的原子性
- **操作日志**：每个任务状态变更都记录 operation_log，可追溯（action, oldStatus, newStatus, details）
- **乐观更新**：前端提交后立即反馈，后台异步处理
- **S3 预签名 URL**：媒体资源通过预签名 URL 访问，安全且高效

### 4. 开发规范
- **类型安全**：全链路 TypeScript，从 API 到组件
- **Next.js 15+ 规范**：
  - 动态路由 params 使用 `Promise<{ id: string }>` 类型
  - 使用 `use()` hook 解包 Promise params
- **Prisma ORM**：类型安全的数据库操作，支持事务和关联查询
- **DaisyUI + Tailwind**：一致的 UI 设计语言
- **ESLint 规范**：
  - 最大行长度：120 字符
  - 无分号风格
  - 自动排序 imports
  - 优先使用模板字符串

### 5. 已实现功能清单

#### Phase 1: 基础架构 ✅
- [x] Next.js 16 App Router 项目初始化
- [x] PostgreSQL + Prisma ORM (with pg adapter)
- [x] JWT 认证系统 (HttpOnly Cookie)
- [x] RBAC 权限控制 (Admin/Labeler/Checker)
- [x] AWS S3 集成 (预签名 URL)
- [x] 数据库种子数据 (seed.ts)

#### Phase 2: 核心功能 ✅
- [x] 用户管理 (CRUD)
- [x] 任务包管理 (创建、编辑、删除、批量创建)
- [x] 任务包分配 (支持限制数量，最多 1000)
- [x] 任务包级别领取 (标注员/质检员)
- [x] 标注工作台 (任务包视图，可领取/我的)
- [x] 质检工作台 (任务包视图，待质检/我的)
- [x] 媒体资源管理 (列表、同步 S3、预览)
- [x] Toast 通知系统
- [x] 分页组件 (Pagination)
- [x] 全局媒体预览 (MediaPreview)

#### Phase 3: 标注引擎 ✅
- [x] Canvas 画布基础 (AnnotationCanvas)
- [x] 矩形工具 (RECT)
- [x] 椭圆工具 (ELLIPSE)
- [x] 形状选择和编辑
- [x] 标注元数据 (MetadataPanel)
  - [x] 标注列表展示
  - [x] 备注输入
- [x] 图片标注
- [ ] 视频播放控制
- [ ] 视频帧提取
- [ ] 画面裁切

#### Phase 4: 增强功能 ✅
- [x] 任务状态流转 (PENDING→LABELING→LABELED→CHECKING→APPROVED/REJECTED)
- [x] 操作日志记录 (OperationLog)
- [x] 质检评分系统 (1-5 分)
- [x] 质检反馈意见
- [x] 按状态分 Tab 展示任务
- [x] 任务列表状态筛选
- [x] 提交后自动跳转下一任务
- [x] Toast 通知替代 Alert
- [x] 智能任务流转（质检员自动查询待质检任务）
- [x] 表单验证（无标注时禁止提交）
- [ ] 统计报表详细页面
- [ ] 性能优化

### 6. 核心组件说明

#### 全局组件
- **GlobalUI**: 全局 UI 容器，包含 Toast 和 MediaPreview
- **Toast**: 自定义 Toast 通知系统，支持多种类型和自动关闭
- **MediaPreview**: 全局媒体预览组件，支持图片/视频预览、键盘导航

#### 标注组件
- **AnnotationCanvas**: 标注画布主组件，支持矩形/椭圆绘制
- **MetadataPanel**: 标注元数据面板，展示标注列表、备注、问题标记

#### 页面组件
- **标注员工作台**: `/labeler/workspace` - 任务包列表和标注页面
- **质检员工作台**: `/checker/workspace` - 任务包列表和质检页面
- **管理员后台**:
  - `/admin/users` - 用户管理
  - `/admin/packages` - 任务包管理
  - `/admin/media` - 媒体资源管理

### 7. API 接口实现

#### 已实现接口
- ✅ `/api/auth/*` - 认证相关
- ✅ `/api/users/*` - 用户管理
- ✅ `/api/packages/*` - 任务包管理
- ✅ `/api/packages/:id/claim` - 任务包领取
- ✅ `/api/packages/:id/distribute` - 任务包分配
- ✅ `/api/tasks/*` - 任务管理
- ✅ `/api/tasks/:id/claim` - 任务领取
- ✅ `/api/tasks/:id/submit` - 标注提交
- ✅ `/api/tasks/:id/review` - 质检审核
- ✅ `/api/media/*` - 媒体资源管理
- ✅ `/api/media/presigned/:key` - 预签名 URL
- ✅ `/api/statistics/overview` - 统计概览

### 8. 待优化项
- [ ] 任务包进度实时更新（WebSocket）
- [ ] 标注数据导出（COCO/VOC 格式）
- [ ] 视频标注增强（时间轴、关键帧）
- [ ] 统计报表详细页面
- [ ] 协作功能（评论、@提及）
- [ ] 移动端适配
- [ ] 性能优化（虚拟滚动、图片懒加载）
- [ ] 批量操作（批量删除、批量分配）

## 🎯 最近更新 (2025-12-30)

### 功能优化
- ✅ **移除 issues 字段**：简化任务元数据结构，移除问题标记功能
- ✅ **任务状态筛选**：任务列表页面支持按状态分 Tab 展示（标注中/已标注/已驳回/已通过等）
- ✅ **优化统计面板**：移除"已标注"统计项，避免与"已驳回"和"已通过"重复
- ✅ **智能任务跳转**：
  - 标注员提交后自动跳转到下一个待标注任务，无下一任务则返回任务列表
  - 质检员点击"继续质检"优先查询质检中（CHECKING）状态的任务
- ✅ **表单验证增强**：标注页面无标注时禁止提交，防止空白任务提交
- ✅ **Toast 通知系统**：全面替换 alert 弹窗为友好的 Toast 通知
- ✅ **质检评分修复**：正确显示已保存的质检评分数据

### 技术改进
- ✅ 数据库迁移：移除 task_metadata 表的 issues 字段
- ✅ Prisma Client 缓存刷新：修复列不存在的错误
- ✅ API 查询优化：支持按状态筛选任务

### 用户体验提升
- ✅ 提交成功后无需手动选择下一任务，自动跳转
- ✅ 统一使用 Toast 通知，减少侵入式弹窗
- ✅ 任务列表支持快速状态筛选，提高查找效率
- ✅ 质检员工作流优化，自动定位到正在进行的质检任务

## 📝 环境配置

```bash
# .env 示例
DATABASE_URL="postgresql://user:password@localhost:5432/label_flow"
JWT_SECRET="your-jwt-secret-key"

# AWS S3 配置
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
AWS_REGION="ap-southeast-2"
S3_BUCKET_NAME="interviewonly"

# 可选配置
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000"
```

## 🚀 快速开始

```bash
# 安装依赖
npm install

# 运行数据库迁移
npx prisma migrate dev

# 生成 Prisma Client
npx prisma generate

# 填充种子数据
npm run db:seed

# 启动开发服务器
npm run dev
```

## 📦 测试账号

```
Admin:   admin@labelflow.com / admin123
Labeler: labeler@labelflow.com / labeler123
Checker: checker@labelflow.com / checker123
```